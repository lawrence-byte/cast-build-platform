# CAST RFI External Links Spec

RFI links use the shared `ExternalLinks` model. Each RFI may reference `issued_rfi_link_id` and `responded_rfi_link_id`, both pointing to controlled external link records. Dropbox links may be included as external package links, but the secure system RFI record requires authentication.
