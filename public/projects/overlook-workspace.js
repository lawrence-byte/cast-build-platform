const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const money=n=>Number(n||0).toLocaleString(undefined,{style:'currency',currency:'USD',maximumFractionDigits:0});
const set=(sel,v)=>{const el=document.querySelector(sel); if(el) el.textContent=v;};
function riskClass(v){return String(v||'').toLowerCase().replace(/[^a-z0-9]+/g,'-');}
async function load(){const r=await fetch('/data/projects/overlook/feasibility-sample.json',{cache:'no-store'}); if(!r.ok) throw new Error('Could not load Overlook sample metadata'); return r.json();}
function renderKpis(data){document.querySelector('[data-kpis]').innerHTML=data.kpis.map(k=>`<div class="metric"><strong>${esc(k.value)}</strong><span>${esc(k.label)}</span><div class="caption" style="margin-top:8px">${esc(k.note)}</div></div>`).join('');}
function renderScenarios(data){document.querySelector('[data-scenarios]').innerHTML=data.scenarios.map(s=>`<tr><td><strong>${esc(s.name)}</strong><br><span class="caption">${esc(s.strength)}</span></td><td>${s.units}</td><td>${esc(s.efficiencyShare)}</td><td>${s.avgUnitSf}</td><td>${s.parkingSpaces}</td><td>${s.parkingRatio.toFixed(2)}</td><td>${money(s.monthlyRent)}</td><td>${esc(s.watchout)}</td></tr>`).join('');}
function renderActions(data){document.querySelector('[data-actions]').innerHTML=data.actions.map(a=>`<tr><td><span class="pill ${riskClass(a.priority)}">${esc(a.priority)}</span></td><td><strong>${esc(a.area)}</strong><br>${esc(a.item)}</td><td>${esc(a.owner)}</td><td>${esc(a.due)}</td><td>${esc(a.next)}</td></tr>`).join('');}
function renderEvidence(data){document.querySelector('[data-evidence]').innerHTML=data.evidence.map(e=>`<a href="#evidence"><strong>${esc(e.category)}</strong><span>${esc(e.status)} · ${esc(e.signal)}<br>${esc(e.safeNext)}</span></a>`).join('');}
function renderParking(data){document.querySelector('[data-parking]').innerHTML=data.parkingTracker.map(p=>`<tr><td><strong>${esc(p.scenario)}</strong></td><td>${p.units}</td><td>${p.spaces}</td><td>${p.ratio.toFixed(2)}</td><td><span class="pill ${riskClass(p.risk)}">${esc(p.risk)}</span></td><td>${esc(p.note)}</td></tr>`).join('');}
(async()=>{const data=await load(); set('[data-generated]',data.posture); set('[data-scenario]',data.currentScenario); set('[data-next]',data.summary.recommendedNextStep); renderKpis(data); renderScenarios(data); renderActions(data); renderEvidence(data); renderParking(data); document.querySelector('[data-copy-summary]').addEventListener('click',async()=>{const text=`${data.projectName}: ${data.currentScenario}\nNext: ${data.summary.recommendedNextStep}`; await navigator.clipboard.writeText(text); set('[data-copy-status]','Copied workspace summary.');});})().catch(e=>{document.body.insertAdjacentHTML('afterbegin',`<div class="note">${esc(e.message)}</div>`);});
